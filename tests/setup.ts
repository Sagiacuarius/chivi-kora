// === Chiví Korá — Test Setup ===
// Configuración global para Vitest browser mode.
// No es necesaria para los tests de dominio (environment: node),
// pero se mantiene por compatibilidad con el runner unificado.